# Nitter session tokens for X/Twitter API access
# Format: {"kind": "cookie", "auth_token": "...", "ct0": "...", "username": "...", "id": "..."}
#
# To generate tokens:
# 1. Create a burner X/Twitter account (without 2FA, or with TOTP secret)
# 2. Clone the Nitter repo: git clone https://github.com/zedeus/nitter
# 3. cd nitter && pip install -r requirements.txt
# 4. python3 tools/create_session_browser.py <username> <password> [totp_secret] --append sessions.jsonl
# 5. Add the session JSON to this file under "nitter_sessions"
#
# WARNING: X Corp has sent C&D letters to Nitter instances. Use at your own risk.
# Consider using a VPN when generating tokens.

nitter_sessions: ""
